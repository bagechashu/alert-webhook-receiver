version: '3'

services:
 alert-webhook-receiver:
   image: xianleeti/alert-webhook-receiver:v0.0.2-sendraw
   container_name: alert-webhook-receiver
   ports:
     - "9000:9000"
   environment:
     DING_ROBOT_TOKEN: <your_ding_robot_token>
     DING_ROBOT_SECRET: <your_ding_robot_secret>
   restart: always